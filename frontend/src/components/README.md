# components/

```
components/
  index.js
  ui/           Button, Input, Avatar, Modal…
  layout/       MainLayout, Navbar, ProtectedRoute…
  common/       PageCard, TeacherCard, modals, filters, legal…
  backgrounds/  Galaxy, AnimatedBackground…
```

Import: `import { PageCard, TeacherCard } from '@/components'`

Layout files: `import MainLayout from '@/components/layout/MainLayout'`

Screens (Login, Home…) live in `src/pages/` — not here.
