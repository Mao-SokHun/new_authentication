# Frontend-only phase — mock data (no backend). See docs/FRONTEND_SCOPE.md
VITE_USE_MOCK=false
# Use Vite proxy (same origin) so auth cookies work with the backend
VITE_API_URL=/api
# Direct URL (cookies may fail cross-port from :5173 → :3000):
# VITE_API_URL=http://localhost:3000/api
